# https://taskfile.dev

version: '3'

includes:
  npm:
    taskfile: ../build/crystal
    dir: .
    vars:
      APP: npm
      NAMESPACE: packages
      TARGET: "export"
      CONTEXT: .
      CONTEXTS:
        map:
          src-packages: '.'
          src-apps: '../../apps'
      BASES:
        - builds/base
      ARGS:
        map:
          BUILD_IMAGE: '{{.REGISTRY}}/builds/base:{{.TAG}}'
