diff --git a/apps/deliver/src/be/config/packages/monolog.yaml b/apps/deliver/src/be/config/packages/monolog.yaml
index 753cd0453..9eb184ffc 100644
--- a/apps/deliver/src/be/config/packages/monolog.yaml
+++ b/apps/deliver/src/be/config/packages/monolog.yaml
@@ -1,11 +1,11 @@
 monolog:
-    channels: ['audit_platform', 'audit_delivery_execution']
+    channels: ['audit_platform', 'audit_delivery_execution', 'deprecation']
     handlers:
         system:
             level: debug
             type: stream
             path: "php://stdout"
-            channels: ['!audit_platform', '!audit_delivery_execution']
+            channels: ['!audit_platform', '!audit_delivery_execution', '!deprecation']
             formatter: monolog.formatter.gcloud_json
         audit_platform:
             level: debug
@@ -22,5 +22,9 @@ monolog:
         console:
             type: console
             process_psr_3_messages: false
-            channels: ['!event', '!doctrine', '!console']
+            channels: ['!event', '!doctrine', '!console', '!deprecation']
             formatter: monolog.formatter.gcloud_json
+        deprecation:
+            bubble: false
+            type: "null"
+            channels: [deprecation]   
diff --git a/apps/deliver/src/be/config/services.yaml b/apps/deliver/src/be/config/services.yaml
index d9d1cbf57..99b67dab1 100644
--- a/apps/deliver/src/be/config/services.yaml
+++ b/apps/deliver/src/be/config/services.yaml
@@ -435,12 +435,14 @@ services:
         factory: ['@Google\Cloud\Core\ServiceBuilder', 'pubsub']
         arguments:
             $config:
+                projectId: '%env(GOOGLE_CLOUD_PROJECT)%'
                 transport: 'grpc'
 
     Google\Cloud\Storage\StorageClient:
         factory: ['@Google\Cloud\Core\ServiceBuilder', 'storage']
         arguments:
             $config:
+                projectId: '%env(GOOGLE_CLOUD_PROJECT)%'
                 transport: 'grpc'
 
     League\MimeTypeDetection\MimeTypeDetector:
