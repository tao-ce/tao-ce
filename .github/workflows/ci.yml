on: # rebuild any PRs and main branch changes
  push:


permissions:
  contents: read
  packages: write
  attestations: write
  id-token: write

jobs:
  build-base:
    uses: ./.github/workflows/build-image.yml
    with:
      image: build-base
      target: build-base
