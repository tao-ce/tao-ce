apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: pubsub-ui
  labels:
    kubernetes.io/service-name: pubsub-ui
    kubernetes.io/managed-by: tao-ce
addressType: IPv4
ports:
  - name: pubsub-ui
    protocol: TCP
    port: 80
endpoints:
  - addresses:
    - "10.247.40.18"
---
apiVersion: v1
kind: Service
metadata:
  name: pubsub-ui
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: pubsub-ui
---
