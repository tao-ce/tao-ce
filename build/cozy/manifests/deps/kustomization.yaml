apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: deps

resources:
  - ns.yaml
  - es.yaml
  - firestore.yaml
  - kibana.yaml
  - pgsql.yaml
  - pubsub.yaml
  - pubsub-ui.yaml
  - redis.yaml

